106c106,108
<     user_cookie_save(array_intersect_key($_SESSION['openid']['user_login_values'], array_flip(array('openid_identifier'))));
---
>     if(is_array($_SESSION['openid']['user_login_values'])) {
>       user_cookie_save(array_intersect_key($_SESSION['openid']['user_login_values'], array_flip(array('openid_identifier'))));
>     }
660a663,669
>   watchdog('openid_debug', 'code started', array(), WATCHDOG_NOTICE, 'link');
> 
>   if(!$response['openid.sreg.nickname']) {
>     $response['openid.sreg.nickname'] = $response['openid.sreg.fullname'];
>   }
>   $response['status'] = 'success';
>   //$_SESSION['openid']['user_login_values']['openid_identifier'] = 'https://openid.ee';
662a672
>   watchdog('openid_debug', 'after user external load', array(), WATCHDOG_NOTICE, 'link');
666c676,677
<   if (!isset($account->uid) && variable_get('openid_less_obtrusive_transition', FALSE)) {
---
>   if (!isset($account->uid)) {
>     watchdog('openid_debug', 'trying to set account', array(), WATCHDOG_NOTICE, 'link');
667a679
>     watchdog('openid_debug', 'openid_identifier: ' . $_SESSION['openid']['user_login_values']['openid_identifier'], array(), WATCHDOG_NOTICE, 'link');
671a684
>     watchdog('openid_debug', 'account uid is set', array(), WATCHDOG_NOTICE, 'link');
673a687
>       watchdog('openid_debug', 'isset account id', array(), WATCHDOG_NOTICE, 'link');
676a691
>         watchdog('openid_debug', 'no errors', array(), WATCHDOG_NOTICE, 'link');
695a711,721
> 
>     $form_state['values']['name'] = $response['openid.sreg.nickname'];
>     if(!$response['openid.sreg.email']) {
>       $response['openid.sreg.email'] = str_replace(' ', '', strtolower($response['openid.sreg.nickname'])) . '@eesti.ee';
>     }
>     $form_state['values']['mail'] = $response['openid.sreg.email'];
>     $rand_pass = user_password(16);
>     $form_state['values']['pass']['pass1'] = $rand_pass;
>     $form_state['values']['pass']['pass2'] = $rand_pass;
> 
>     watchdog('openid_debug', 'submitting user registration form', array(), WATCHDOG_NOTICE, 'link');
696a723,740
>     /*
>     if (form_get_errors() != '') {
>       print 'Webform_Error';
>       print '<pre>'.print_r(form_get_errors(), 1).'</pre>';
>     }
>     */
> 
>     watchdog('openid_debug', 'after user registration form submit', array(), WATCHDOG_NOTICE, 'link');
>     watchdog('openid_debug', 'user array: ' . implode($form_state['user']), array(), WATCHDOG_NOTICE, 'link');
>     watchdog('openid_debug', 'user values array: ' . implode($form_state['values']), array(), WATCHDOG_NOTICE, 'link');
>     watchdog('openid_debug', 'user values array: ' . implode(debug_backtrace()), array(), WATCHDOG_NOTICE, 'link');
>     /*
>     print '<pre>';
>     print_r($form_state);
>     print '</pre>';
>     die('<pre>' . print_r(debug_backtrace(), TRUE) . '</pre>');
>     exit;
>     */
699a744,750
>       $authmap = db_insert('authmap')
>         ->fields(array(
>             'uid' => $form_state['values']['uid'],
>             'authname' => $identity,
>             'module' => 'openid',
>         ))->execute();
>       watchdog('openid_debug', 'user creation successful', array(), WATCHDOG_NOTICE, 'link');
707a759,761
>       watchdog('openid_debug', 'name: ' . $form_state['values']['name'], array(), WATCHDOG_NOTICE, 'link');
>       watchdog('openid_debug', 'mail: ' . $form_state['values']['mail'], array(), WATCHDOG_NOTICE, 'link');
>       watchdog('openid_debug', 'user name or mail empty', array(), WATCHDOG_NOTICE, 'link');
721a776
>     watchdog('openid_debug', 'going to user/register', array(), WATCHDOG_NOTICE, 'link');
726a782
>   watchdog('openid_debug', 'user registration form', array(), WATCHDOG_NOTICE, 'link');
