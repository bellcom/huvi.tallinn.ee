<?php

/**
 * @file
 * Code for the Kultuutikava search module.
 */

/**
 * Implements hook_menu().
 */
function kultuurikava_search_menu() {
  $items = array();

  /*
  $items["uritused"] = array(
    'page callback' => 'kultuurikava_search_events',
    'file' => 'kultuurikava_search.pages.inc',
    'page arguments' => array(1),
    'access callback' => TRUE,
    'type' => MENU_NORMAL_ITEM,
  );

  $items["huvitegevused"] = array(
    'page callback' => 'kultuurikava_search_activities',
    'file' => 'kultuurikava_search.pages.inc',
    'page arguments' => array(1),
    'access callback' => TRUE,
    'type' => MENU_NORMAL_ITEM,
  );
  */

  $items["uritused"] = array(
    'page callback' => 'kultuurikava_empty_page',
    'access callback' => TRUE,
    'type' => MENU_NORMAL_ITEM,
  );

  $items["huvitegevused"] = array(
    'page callback' => 'kultuurikava_empty_page',
    'access callback' => TRUE,
    'type' => MENU_NORMAL_ITEM,
  );

  return $items;

}


function kultuurikava_empty_page() {
  return '';
}


/**
 * Implements hook_theme().
 */
function kultuurikava_search_theme() {
  return array(
    'kultuurikava_search_events' => array(
      'arguments' => array('data' => NULL),
      'template' => 'templates/kultuurikava_search_events',
    ),
    'kultuurikava_search_activities' => array(
      'arguments' => array('data' => NULL),
      'template' => 'templates/kultuurikava_search_activities',
    ),
  );
}

function kultuurikava_search_update_7100() {
  db_query('ALTER TABLE `field_data_field_schedule_asukoht` CONVERT TO CHARACTER SET utf8 COLLATE utf8_bin;');
  db_query('ALTER TABLE `field_data_field_schedule_city_id` CONVERT TO CHARACTER SET utf8 COLLATE utf8_bin;');
  db_query('ALTER TABLE `field_data_field_schedule_date` CONVERT TO CHARACTER SET utf8 COLLATE utf8_bin;');
  db_query('ALTER TABLE `field_data_field_schedule_location` CONVERT TO CHARACTER SET utf8 COLLATE utf8_bin;');
  db_query('ALTER TABLE `field_data_field_schedule_place` CONVERT TO CHARACTER SET utf8 COLLATE utf8_bin;');
  db_query('ALTER TABLE `field_data_field_schedule_toimumiskoht` CONVERT TO CHARACTER SET utf8 COLLATE utf8_bin;');
  db_query('ALTER TABLE `field_data_field_schedule_toimumis` CONVERT TO CHARACTER SET utf8 COLLATE utf8_bin;');
  db_query('ALTER TABLE `field_data_field_schedule` CONVERT TO CHARACTER SET utf8 COLLATE utf8_bin;');
  db_query('ALTER TABLE `field_data_field_description` CONVERT TO CHARACTER SET utf8 COLLATE utf8_bin;');
  db_query('ALTER TABLE `node` CONVERT TO CHARACTER SET utf8 COLLATE utf8_bin;');
}